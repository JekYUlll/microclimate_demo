# 固化调度模型

本目录存放 Simulink 调度模块使用的固化模型文件。

文件说明：

```text
pdppo_demo_policy.mat   MATLAB/Simulink 直接加载的模型参数
pdppo_demo_policy.json  同一模型参数的可读版本，便于查看和讲解
```

该模型是一个轻量、确定性的 PD-PPO 风格调度策略。它保留了论文方法的运行时结构：

```text
传感器系统状态 -> 通道评分 -> 功率预算和运行规则投影 -> 可执行通道开关
```

演示时不进行训练，只加载这里的固定参数并输出 8 路通道开关。
