# 固化补全模型

本目录存放 Python 后处理脚本使用的固化通道重构模型。

文件说明：

```text
channel_reconstructor.npz
```

该模型用于把 Simulink 生成的残缺观测数据补全为完整通道序列。模型参数是固定的，
演示时只做推理，不进行训练。

补全逻辑包括：

- 利用最近时刻的重构状态；
- 利用当前已经观测到的通道；
- 利用固定的通道之间耦合关系；
- 对事件相关通道给予额外响应。
