# 演示输入数据目录

本目录用于存放 MATLAB 生成的演示输入数据。

运行：

```matlab
START_HERE
```

之后会生成：

```text
demo_inputs.mat
```

该文件包含一个确定性的 17 维输入时间序列，用于驱动 Simulink 调度模型。

17 维输入含义：

```text
1--8   归一化后的传感器系统通道状态
9--16  8 个通道的 AoI 或缺失时长
17     事件状态，0 表示普通时段，1 表示事件时段
```
